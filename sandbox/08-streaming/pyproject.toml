[project]
name = "ch08-streaming"
version = "0.1.0"
description = "Chapter 8: Streaming - text deltas and partial structured output."
requires-python = ">=3.12"
dependencies = ["pydantic-ai>=1.0", "pai-sandbox-shared"]
